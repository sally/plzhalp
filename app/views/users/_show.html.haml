%div
  %span
    %h2
      = @user.full_name

%div
  %span
    = @user.role

%div
  %span
    = @user.email

- if @user.role == "mentor"
  %div
    Strengths:
    %div
    Phase 1:
    - Mentor.find_by(user: @user).phase_strengths(1).each do |strength|
      %span
        = strength
    %div
    Phase 2:
    - Mentor.find_by(user: @user).phase_strengths(2).each do |strength|
      %span
        = strength
    %div
    Phase 3:
    - Mentor.find_by(user: @user).phase_strengths(3).each do |strength|
      %span
        = strength
  %div
    Total Sessions Completed:
    = Mentor.find_by(user: @user).total_completed_appointments

- if @user.role == "student"
  %div
    Phase:
    = Student.find_by(user: @user).phase.number

%object{:height => "0", :width => "0"}
  %param{:name => "movie", :value => "http://www.youtube.com/v/d9TpRfDdyU0?fs=1&hl=en_US&rel=0"}
  %param{:name => "allowFullScreen", :value => "true"}
  %param{:name => "allowscriptaccess", :value => "always"}
  %embed{:allowfullscreen => "true", :allowscriptaccess => "always", :height => "390", :src => "http://www.youtube.com/v/d9TpRfDdyU0?fs=1&hl=en_US&rel=0", :type => "application/x-shockwave-flash", :width => "640"}